version: "3.9"

services:
  bot:
    build: .
    container_name: bikahelpbot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./:/app
    command: python -m app.main
    ports:
      - "8080:8080"
